<Page
    x:Name="ThisPage"
    x:Class="Microsoft.Toolkit.Uwp.Notifications.DemoChatApp.ViewConversationPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Microsoft.Toolkit.Uwp.Notifications.DemoChatApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <ListView
            ItemsSource="{Binding Conversation.Messages, ElementName=ThisPage}">
            <ListView.Header>
                <TextBlock
                    Text="{Binding Conversation.With.Name, ElementName=ThisPage}"
                    Style="{ThemeResource SubtitleTextBlockStyle}"
                    Margin="20"/>
            </ListView.Header>
            <ListView.ItemTemplate>
                <DataTemplate>
                    <TextBlock
                        Text="{Binding Content}"
                        Style="{ThemeResource BodyTextBlockStyle}"
                        Margin="0,0,0,20"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        
    </Grid>
</Page>
